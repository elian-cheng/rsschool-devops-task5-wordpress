namespace: wordpress

wordpress:
  image: "wordpress:latest"
  replicaCount: 1
  service:
    type: NodePort
    port: 80
    nodePort: 32000
  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"
    limits:
      memory: "512Mi"
      cpu: "200m"
  persistentVolume:
    size: 1Gi

postgresql:
  enabled: true
  image: "postgres:13"
  postgresPassword: "postgres"
  database: "wp_db"
  user: "wp_user"
  password: "wp_password"
  persistentVolume:
    size: 1Gi
